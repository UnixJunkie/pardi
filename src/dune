
(library
  (name sec_chan)
  (modules DHKE crypto)
  (libraries cryptokit batteries dolog))

(executables
  (names        pardi)
  (public_names pardi)
  (modules pardi flags utls mux demux sorted_queue)
  (libraries batteries dolog parany minicli str sec_chan))
