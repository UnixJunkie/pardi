
(library
  (name sec_chan)
  (modules DHKE crypto)
  (libraries cryptokit))

(executables
  (names        pardi)
  (public_names pardi)
  (modules pardi utls flags mux demux sorted_queue)
  (libraries batteries dolog parany minicli str sec_chan))
